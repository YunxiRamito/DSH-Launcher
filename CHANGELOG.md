# 更新日志

发版时 `release.ps1` 会自动抓取对应版本的小节,写进 `manifest.json` 的 `notes`,
`publish-release.ps1` 再把它作为 GitHub Release 的正文。所以**发版前先在这里写好这一版**。

格式固定:`## 1.3.18` 开头,下一行到下一个 `## ` 之间就是这一版的说明。

---

## 1.4.9.1

**修复版**

### DSH 启动器

- 修复代理设置重启后变成“不使用代理”的问题。
- DSH 更新安装阶段改用与安装器一致的 `node_modules` 体积估算进度。
- 插件安装改为单一进度条：下载完成到 70%，安装完成到 100%，完成后按钮显示“已安装”。
- 插件安装中的按钮只置灰，不再在按钮文字里显示百分比。
- 启动器更新会先更新同版本安装器和卸载器，再替换启动器并重启，成功后清理旧副本。
- 主题页新增“窗口风格”，可选择 Windows 10 直角扁平或 Windows 11 圆角 Fluent 样式。
- Windows 10 的插件和组件导航图标改为描边版本。

---

## 1.4.9

**Dafeiyu-Go 过渡升级版本**

> 本版合并了此前未正式发布的 `1.4.3` 功能。

### DSH 启动器

- 产品更名为“大肥鱼Go / Dafeiyu-Go”，安装器与启动器仓库完成新名称迁移。
- 新增代理设置，可接入本地 Proxy 软件，改善中国大陆网络环境下的下载与访问体验。
- 新增 DSH 插件板块，支持分类、搜索、排序、分页和缓存，并支持插件详情、安装进度、本地目录、卸载、单个更新和全部更新。
- 新增组件板块，支持检测和安装 .NET、Windows App Runtime、Node、MinGit、pnpm、Python。
- 修复若干已知问题并提升稳定性。

### 安装程序

- 产品更名为“大肥鱼Go / Dafeiyu-Go”，安装器与启动器仓库完成新名称迁移。
- 安装器升级 Mica Alt，并跟随 Windows 主机亮暗主题；不支持时自动回退 Acrylic。
- 修复组件页、步骤条、卡片、完成页和卸载页的深色主题适配。

---

## 1.4.3

**插件中心、代理与开发者后台**

- 新增直连、系统代理和自定义 HTTP/HTTPS/SOCKS5 三种网络模式，更新、插件目录、余额和头像请求统一接入
- 新增 DSH 插件市场与 GitHub 搜索两级来源，支持分类、已验证筛选、搜索、排序、分页和磁盘缓存
- 在线插件安装改用市场提供的可执行安装候选，支持固定提交 SHA 的 GitHub 来源和 npm 来源
- 插件卡片支持详情、安装进度、本地目录、卸载，以及单个和全部更新检查
- 新增插件更新策略、更新提醒和更新完成通知；通知里的“好的”会重启 DSH，稍后再说保持当前服务
- 官方推荐页改读仓库中的 `featured-plugins.json`，不再使用硬编码样例
- 新增本地开发者管理中心，支持 GitHub 设备码或 Token 登录、云端角色名单、推荐列表增删改和提交到仓库
- 新增组件页，支持检测和安装 .NET、Windows App Runtime、Node、MinGit、pnpm、Python
- 便携组件的 Node、Git、pnpm、Python 目录会写入当前用户 PATH
- 设置页补全代理配置、GitHub Token、插件来源、插件更新和组件管理界面

---

## 1.4.2

**Win10 图标与更新体验修正**

- Windows 10 设置导航改用微软 Fluent System Icons SVG，并按浅色/深色主题加载对应图标
- 启动器更新和 DSH 更新统一右下角进度窗的标题、下载、安装和重启状态
- DSH 更新检查优先比较 npm 发布时间，正确识别 `0.1.5-rc.1` 到 `rc.2` 这类预发布版本
- DSH 更新兼容内置 Node、独立 Node、DSH `.bin` 和系统 PATH 中的 npm，并修正 Windows cmd 参数引号
- 自动安装更新已到期时先更新、后启动 DSH 服务，避免先起旧服务再中断更新
- 仅检查更新模式不影响正常启动；服务停止时托盘和设置页显示“启动服务”
- 持续探测 DSH 服务状态，外部停止或重新启动服务后托盘和设置页会自动同步
- 修复退出时 Mica 清理和 WinUI `Application.Exit()` 触发 `0xc0000005` 原生访问冲突的问题
- DSH 更新改为先下载 npm tgz，再执行本地部署；下载阶段显示真实字节进度
- 启动器或 DSH 更新完成后，只显示更新结果通知，不再叠加本次服务启动成功通知

---

## 1.4.1

**设置与更新体验修正**

- 修复退出时偶发的异常弹窗
- 启动器和 DSH 更新改为官方 `SettingsExpander` 子项
- 手动检查更新时显示检测、下载和安装状态，设置窗关闭后更新继续执行
- 按 Windows 版本统一圆角：Windows 11 使用圆角，Windows 10 使用直角
- 修复下拉框方框样式、按钮圆角和固定端口输入框失焦问题
- API 设置打开时自动加载当前余额
- 托盘提示显示运行状态和剩余余额
- 关于页面加入作者信息、社交链接和收款码
- 作者头像在首次打开时通过 B 站 API 加载并缓存到进程

---

## 1.4.0

**新增 WinUI3 设置中心**

- 新增常规、主题、API、提醒、服务、更新和关于页面
- 设置即时保存到 `%LOCALAPPDATA%\DeepSeekHarness\LauncherSettings.json`
- 主题、主题色来源和窗口材质会在设置窗、托盘菜单、更新进度窗和通知窗之间同步
- 新增固定端口、随机端口和默认端口策略，默认使用固定端口 8787
- 新增开机自启动、静默启动、更新源、更新周期和提醒阈值设置
- 新增启动器与 DSH 更新策略，支持自动安装、仅检查更新和关闭
- 新增 API Key 验证状态、余额状态、充值提醒和分层余额提醒
- 新增 DSH 目录和 Node 路径配置，并同步安装器卸载记录
- 托盘菜单改为“余额、打开页面、重启服务、强制停止、检查更新、设置、退出”
- 旧版 API Key、`launcher.json` 和开机自启动状态会在首次启动时迁移

---

## 1.3.21

**更新失败要"快速换源",不能干等**

- 单个源的连接超时压到 15 秒,整体读写超时压到 3 分钟
- 之前是 10 分钟:遇到连不上的镜像(`ghfast.top` 在国内实测超时),
  客户端会对着进度条卡十分钟才换下一个源
- 现在几十秒内就会跳到下一个可用源

---

## 1.3.20

**修复进度窗无法创建**

- WinUI 窗口必须在 UI 线程创建。更新跑在后台线程,直接 new 会拿到
  `RPC_E_WRONG_THREAD (0x8001010E)`,进度窗一直弹不出来
- 现在窗口的创建与刷新都排到 UI 线程执行,并等它完成再继续

**更新源按国内实测重排**

- 实测结果(2026-09-19):`api.github.com` 600ms 通、`ghproxy.net` 1.1s 通、
  `ghfast.top` 超时、`raw.githubusercontent` 超时、jsDelivr SSL 失败
- 清单源改为官方 API 优先(附带把 releases/latest 结构转换成内部清单格式),
  再依次退到镜像、raw、jsDelivr
- zip 资产的下载同样按这个顺序套加速前缀

---

## 1.3.19

**修掉启动期自更新被进度窗卡死的问题**

- 启动期自更新入口里,`_updateWindow.Update(...)` 改写成安全包装版时误写成了自递归,
  调用直接爆栈,更新流程还没读到版本清单就中断了(日志里表现为一行空的「启动期更新出错:」)
- 现在进度窗的创建与刷新都隔离:窗口建不出来也照常更新,只是没有进度条
- 异常日志补上类型名 / 内部异常 / HRESULT,不再出现空白错误

---

## 1.3.18

**菜单高度自适应**

- 菜单不再写死高度。之前加了「检查更新」之后固定高度没跟上,最后一项「退出」被裁在屏幕外
- 改成每次弹出时按内容量高度,以后加菜单项 / 改字号 / 换 DPI 缩放都不用再手改常量

**清单缓存策略修正**

- jsDelivr 对固定的清单路径有约 12 小时缓存。旧客户端查新版时读到的是缓存的旧清单,
  会误判成「已经最新」——这个坑在测试机上真实踩到过
- 现在清单一律以 `raw.githubusercontent.com` 为首选(实时),jsDelivr 只作兜底
- 真正的加速挪到不可变的 zip 资产那一侧(GitHub 资产套 `ghfast.top` / `ghproxy.net` 前缀)

---

## 1.3.16

**自更新(强制、静默)**

- 启动 4 秒后自动检查版本,发现新版直接更新,不询问
- 右下角 WinUI 小窗显示进度:云母背景、进度条、已下载/总量/百分比
- 下载完成后由独立脚本替换文件(正在运行的 exe 没法覆盖自己)
- **只重启启动器,DSH 服务进程不动**
- 重启不带浏览器,并由重启后的实例推一条「更新完成」通知

**端口自动识别**

- 不再写死 8787。每次启动按顺序判定:
  运行中 dsh 进程的 `--port` → DSH 自己写的 `logs\last-url.txt` → 常见端口探测 →
  `launcher.json` → 自己挑一个空闲端口
- DSH 输出的带 token 地址改成正则抓取,并跟随其中的真实端口

**不会重复起服务**

- 启动时先探测 DSH 是否已在运行;端口被占但还没响应时先等它,不抢着起第二个进程

**其它**

- 修掉「检查更新」菜单项的悬停高亮(漏加进主题笔刷数组)
- 自更新相关的通知全部移除,只剩右下角进度窗
- 更新后重启改走计划任务(最高权限),静默把托盘拉回来,不再触发 UAC

---

## 1.3.14

**开机自启改用计划任务**

- 启动器清单声明 `requireAdministrator`,而 Windows 在登录阶段不弹 UAC,
  被启动文件夹 / Run 键拉起来的进程会被静默拦掉 —— 自启等于没写
- 改注册计划任务(登录触发 + 最高权限 + 交互式),由任务计划服务用管理员令牌启动
- 升级后首次打开自启时,会自动清理旧版写下的 Run 键与启动文件夹快捷方式

---

## 1.3.13

**可移植性(装到别人机器上不再翻车)**

- 新增 `LauncherLocator`:DSH 根目录与 node 都动态查找
  (`DSH_ROOT` → 同目录 `launcher.json` → 向上找 `bin.js` → 常见位置 → 记录文件)
- 删除写死的开发机路径;找不到就明确报错,而不是去开一个不存在的目录
- 日志统一到 `%LOCALAPPDATA%\DeepSeekHarness`

**启动体验**

- 新增启动前自检:缺 Node 或 DSH 时弹中文对话框并给出日志路径
- 修掉「打开页面」拿到 401:token 地址是异步写出的,现在最多等 25 秒,
  并在点击时重新读取
- 托盘菜单图标加字体档位探测:Win11 用 `Segoe Fluent Icons`,Win10 退到 `Segoe MDL2 Assets`

---

## 1.3.9

- 首个公开发布版本
