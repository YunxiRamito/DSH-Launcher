# 构建产物
source/bin/
source/obj/
source/dist/
source/dist-*/
source/publish/
dist/
DeepSeekHarness-*/

# 发版打包出来的 zip(作为 release 资产上传,不进仓库)
*.zip

# 早期 WinForms 版的编译产物,只是留个纪念,源码在 legacy\*.cs
source/legacy/*.exe

# NuGet
*.nupkg
packages/

# 编辑器 / 本机
.vs/
.idea/
*.user
*.suo
*.log
Thumbs.db
desktop.ini

# jsDelivr 只能取**仓库里的文件**,取不了 Release 资产 ——
# 所以镜像要用的那份 zip 必须进仓库(其余 *.zip 仍然排除)。
# 路径和 tag 一起变:见 RELEASE.md 的"jsDelivr 镜像"一节。
!assets/*.zip
